{
  "name": "mobile-agents",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "claude": "node .github/scripts/run-claude.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
